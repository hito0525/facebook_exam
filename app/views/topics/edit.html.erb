<%= form_for(@topic) do |f| %>
  <%= f.label :title %>
  <%= f.text_field :title %>
  <br>
  <%= f.label :content %>
  <%= f.text_field :content %>
  <br>
  <%= f.submit  data: { confirm: '本当に編集していいですか？'} %>
<% end %>

<%= link_to "トピックス一覧画面にもどる", topics_path %>

